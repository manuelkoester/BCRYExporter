name: Release

on:
  push:
    tags:
    - '*'

jobs:
  release:
    name: Release
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Upload release
        uses: thedoctor0/zip-release@master
        with:
          artifacts: "BCRYExporter.zip"
          directory: ./io_bcry_exporter
          token: ${{ secrets.GITHUB_TOKEN }}